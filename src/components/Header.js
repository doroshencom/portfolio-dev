import React from 'react';
import './Header.css';  // Archivo CSS

const Header = () => {
  return (
    <header className="header-container">
      <h1 className="title">SHENKO</h1>
    </header>
  );
};

export default Header;
